// Redirecionando para as páginas quando clico em alguns botões
function redireciona(page) {
    window.location.href = page
}

// Colocando foco automático no campo nome do cadastro do funcionário
document.addEventListener('DOMContentLoaded', function() {
    document.getElementById('nome').focus();
});

function excluiHorario() {
    
}